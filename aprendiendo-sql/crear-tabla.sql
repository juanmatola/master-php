//SENTENCIAS EN SQL

// Crear tabla
CREATE TABLE usuarios(
    id          int(11),
    nombre      varchar(100),
    appelido    varchar(100),
    email       varchar(100),
    password    varchar(100)
);

// mostrar tablas: show tables;

//Eliminar tabla
drop table usuarios;